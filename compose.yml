# Variables (set them in the .env / Portainer stack env vars):
#   SSD=/mnt/ssd/Tools          -> base folder for the data (SSD dataset)
#   PASS=YourStrongPassword     -> Perforce 'admin' super-user password
#   TZ=America/Sao_Paulo
#
# NOTE: the container runs as root, so it writes to ${SSD}/perforce
# regardless of the host folder ownership.
networks:
  perforce:
    external: false
    name: perforce

services:
  perforce:
    image: finallf/perforce:latest
    container_name: perforce
    hostname: perforce
    environment:
      - TZ=${TZ}
      - P4USER=admin
      - P4PORT=1666
      - P4PASSWD=${PASS}
      - P4CHARSET=utf8
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - /usr/share/zoneinfo:/usr/share/zoneinfo:ro
      - ${SSD}/perforce:/p4
    ports:
      - "1666:1666"
    networks:
      - perforce
    stop_grace_period: 30s
    restart: unless-stopped
